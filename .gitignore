node_modules/
.env

# Agent operational memory (regenerated locally, not version-controlled)
.agent-memory/
*.log

# Local model files (GGUF etc.) — never commit large model weights.
# Keep the agent model-agnostic; download models out of band.
*.gguf
*.gguf.*
models/

# Runtime state managed by the `go` launcher (PID files, logs).
.agent-runtime/
